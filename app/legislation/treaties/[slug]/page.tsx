import {notFound} from "next/navigation";import {getLegislationDocument,getProvisions,getVersions} from "@/lib/legislation/queries";import {LegislationDocumentView} from "@/components/legislation/LegislationDocumentView";
export default async function Page({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const doc=await getLegislationDocument(slug,"treaty");if(!doc)notFound();const [provisions,versions]=await Promise.all([getProvisions(doc.legislation_document_id),getVersions(doc.legislation_document_id)]);return <div className="govuk-width-container"><LegislationDocumentView document={doc} provisions={provisions} versions={versions}/></div>}
