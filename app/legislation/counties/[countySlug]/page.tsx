import { notFound } from "next/navigation";import { getCounties,listLegislation } from "@/lib/legislation/queries";import { LegislationList } from "@/components/legislation/LegislationList";import { LegislationFilters } from "@/components/legislation/LegislationFilters";
export default async function Page({params,searchParams}:{params:Promise<{countySlug:string}>,searchParams:Promise<Record<string,string|undefined>>}){const [{countySlug},p,counties]=await Promise.all([params,searchParams,getCounties()]);const county:any=counties.find((c:any)=>c.slug===countySlug);if(!county)notFound();const items=await listLegislation({category:"county_act",countyCode:county.code,query:p.query,status:p.status});return <main className="govuk-width-container govuk-main-wrapper" id="main-content"><span className="govuk-caption-l">County legislation</span><h1 className="govuk-heading-xl">{county.official_name}</h1><div className="govuk-grid-row"><div className="govuk-grid-column-one-third"><LegislationFilters action={`/legislation/counties/${countySlug}`} values={p}/></div><div className="govuk-grid-column-two-thirds"><p className="govuk-body-s">{items[0]?.total_count ?? 0} results</p><LegislationList items={items}/></div></div></main>}
