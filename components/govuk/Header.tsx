/**
 * @deprecated Use SiteHeader from `@/components/site` instead.
 *
 * The site header is custom Kenya product chrome, not a GOV.UK component.
 * This re-export remains so old imports keep working.
 */
export { default } from "@/components/site/SiteHeader";
